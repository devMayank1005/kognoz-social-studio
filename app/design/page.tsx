import { DesignSystemView } from "./DesignSystemView";

// The design system reference — type scale, font-loading rules, design families and
// palettes, all read from the live tables rather than transcribed.
export default function DesignPage() {
  return <DesignSystemView />;
}
