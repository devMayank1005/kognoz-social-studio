import { VoiceView } from "./VoiceView";

// The voice guide — real human writing that every generation imitates.
// Maintained by whoever is signed in; `addedBy` records who contributed each sample.
export default function VoicePage() {
  return <VoiceView />;
}
